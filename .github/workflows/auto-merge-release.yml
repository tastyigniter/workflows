name: Auto Merge Release PR & Create Release

on:
  workflow_call:
    inputs:
      release-branch:
        description: "Branch where release PRs should be merged"
        required: false
        default: "main"
        type: string

jobs:
  debug-context:
    name: 🐛 Debug Context
    runs-on: ubuntu-latest
    # Always run, no conditions
    steps:
      - name: Dump event data
        continue-on-error: true
        run: |
          echo "🔎 Event name: ${{ github.event_name }}"
          echo "🔎 Ref: ${{ github.ref }}"
          echo "🔎 Head ref: ${{ github.head_ref }}"
          echo "🔎 Base ref: ${{ github.base_ref }}"
          echo "🔎 PR title: ${{ github.event.pull_request.title }}"
          echo "🔎 PR base ref: ${{ github.event.pull_request.base.ref }}"
          echo "🔎 Draft status: ${{ github.event.pull_request.draft }}"
          echo "🔎 Release branch input: ${{ inputs.release-branch }}"
          echo "🔎 Conditions to match:"
          echo "    - event_name == 'pull_request'"
          echo "    - base.ref == inputs.release-branch"
          echo "    - draft == false"
          echo "    - title starts with 'Release v'"
          echo "---------------------------------------"
          echo "If this job runs but 'auto-merge' is skipped,"
          echo "it means one or more of these values don't match."

  auto-merge:
    name: Auto Merge Release PR
    runs-on: ubuntu-latest

    # Run only for "Release vX.Y.Z" PRs into the correct branch
    if: >
      github.event_name == 'pull_request' &&
      github.event.pull_request.base.ref == inputs.release-branch &&
      github.event.pull_request.draft == false &&
      startsWith(github.event.pull_request.title, 'Release v')

    permissions:
      contents: write
      pull-requests: write
      checks: read
      actions: read
      statuses: read

    outputs:
      version: ${{ steps.extract_version.outputs.version }}

    steps:
      - name: Extract version from PR title
        id: extract_version
        run: |
          title="${{ github.event.pull_request.title }}"
          if [[ "$title" =~ ^Release[[:space:]]v([0-9]+\.[0-9]+\.[0-9]+)$ ]]; then
            version="v${BASH_REMATCH[1]}"
            echo "✅ Extracted version: $version"
            echo "version=$version" >> $GITHUB_OUTPUT
          else
            echo "❌ PR title does not match 'Release vX.Y.Z'"
            exit 1
          fi

      - name: Wait for all required checks to pass
        uses: lewagon/wait-on-check-action@v1.3.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          wait-interval: 15
          running-workflow-name: Auto Merge & Create Release

      - name: Auto-merge the PR
        id: automerge
        if: ${{ success() }}
        uses: pascalgn/automerge-action@v0.16.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MERGE_METHOD: merge
          MERGE_COMMIT_MESSAGE: "Auto-merged Release PR"
          MERGE_DELETE_BRANCH: false

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [auto-merge]
    if: ${{ needs.auto-merge.outputs.version != '' }}

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create GitHub Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.auto-merge.outputs.version }}
          release_name: Release ${{ needs.auto-merge.outputs.version }}
          body: ${{ github.event.pull_request.body }}
          draft: false
          prerelease: false
